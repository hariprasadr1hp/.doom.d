;;; modes/org-mode/+org_export.el -*- lexical-binding: t; -*-

; (require 'org)
;
; (defun read-org-file-contents (file-path)
;   "Read and display the contents of an Org file specified by FILE-PATH."
;   (interactive "fPath to org file: ") ;; Prompts for the file path
;   (with-temp-buffer
;     (insert-file-contents file-path)
;     ;; Optionally, you can perform some operations on the buffer here
;     (message "%s" (buffer-string)))) ;; Print the contents in the minibuffer
;
